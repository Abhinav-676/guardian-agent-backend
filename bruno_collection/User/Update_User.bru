meta {
  name: Update User
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/users/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "name": "Updated Name"
  }
}
